{
  "name": "omnibook",
  "version": "1.0.0",
  "private": true,
  "description": "OmniBook — WhatsApp-native appointment booking. Cloudflare Workers + Supabase Postgres.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:push": "supabase db push",
    "fn:deploy": "supabase functions deploy omnibook-api --no-verify-jwt"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
